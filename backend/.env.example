# MongoDB Atlas (use your actual password and database name)
# Replace <db_password> with your database user password. If password has special chars, URL-encode them (e.g. @ -> %40)
MONGO_URI=mongodb+srv://f223873:orhan@5000@neurohire.nftaz6m.mongodb.net/neurohire?retryWrites=true&w=majority

# Server
PORT=5000

# Frontend URL (for CORS) - update when deploying
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000

# Backend URL - update when deploying
BACKEND_URL=http://localhost:5000

# Bytez/LLM (optional)
BYTEZ_API_KEY=your_bytez_api_key

# Admin JWT (optional, for admin panel)
ADMIN_JWT_SECRET=your_random_secret

# n8n webhooks (optional)
# N8N_EMAIL_WEBHOOK_URL=
# N8N_WEBHOOK_URL=
